<h1>Поиск билета</h1>

<div class="row ">
  <%= form_tag search_path do %>
  <div class="col-md-4">
    <%= select_tag :start_station, options_from_collection_for_select(@station, :id, :title), class: 'form-control' %>
  </div>
      <div class="col-md-4">
<%= select_tag :end_station, options_from_collection_for_select(@station, :id, :title),  class: 'form-control' %>
      </div>
      <div class="col-md-4">
<%= submit_tag "Найти", class: 'bnt btn-primary' %>
      </div>
  <% end %>
</div>